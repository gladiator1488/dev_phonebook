services:
  db:
    image: postgres:15

    env_file:
      - .env

    volumes:
       - pgdata:/var/lib/postgresql/data


  web:
    build: .
    ports:
      - "8080:5000"

    depends_on:
      - db
    
    env_file:
      - .env

volumes:
  pgdata:
